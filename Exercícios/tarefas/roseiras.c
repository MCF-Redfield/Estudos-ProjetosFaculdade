#include<stdio.h>
main()
{
      int qtde,dist=1,i,ida,soma=0;
      
      printf("\nDigite o numero da quantidade de roseiras: ");
      scanf("%d",&qtde);
           if(qtde%3==0)
           ida=qtde/3;
           else
               if(qtde%3!=0)
               ida=(qtde/3)+1;
               for()
               {
               soma=soma+(3*ida*2);
               soma++;
               }
               printf("\n a distancia percorrida eh:%d",soma);
getchar();
getchar();
}
